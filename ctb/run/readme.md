# Run
ctb, 50 points

> Мы нашли уязвимый сервер: кажется, на порту 5001 на сервере ctb.upmlctf.ru поднят SSH! Логин run, пароль run
>
> Узнайте, как туда подключиться и получить флаг

## Writeup

Подключаемся любым SSH-клиентом. Выполняем `ls`. Видим
в директории `flag`. Пробуем `file flag`, это бинарник.
Запускаем: `./flag` и получаем флаг.

Флаг: **uctf_bash_is_so_easy**
